// PHASE 1 STUB — placeholder route so auth redirect doesn't 404.
// Phase 2 will replace this with a real login form wired to POST /api/auth/login.
export default function Login() {
  return (
    <div className="fixed inset-0 flex items-center justify-center bg-background">
      <div className="flex flex-col items-center gap-3">
        <span className="text-sm text-muted-foreground">Login coming in Phase 2.</span>
      </div>
    </div>
  );
}
